name: Test macOS image
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  test:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - run: ls
    - name: Run a one-line script
      run: ./processes-with-open-windows
